import { createStore } from 'vuex'

import HomeModule from "./modules/HomeModules"

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    HomeModule
  }
})
